dataset:
  target: 7
  split: [0.8, 0.1, 0.1]
  seed: 42
  feature_mode: topology

training:
  epochs: 200
  batch_size: 32
  lr: 1.0e-3
  scheduler: cosine
  patience: 20

model:
  hidden_dim: 128
  num_layers: 4
  dropout: 0.0
  heads: 8
